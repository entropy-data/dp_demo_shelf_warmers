name: 'shelf_warmers'
version: '1.0.0'

profile: 'shelf_warmers'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  shelf_warmers:
    +materialized: table
    staging:
      +materialized: view
    intermediate:
      +materialized: view
