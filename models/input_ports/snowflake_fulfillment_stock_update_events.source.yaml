version: 2
sources:
  - name: stock-update-events_snowflake_fulfillment_stock_update_events
    database: DP_STOCK_UPDATE_EVENTS
    schema: OP_STOCK_UPDATE_EVENTS_V1
    meta:
      data_contract:
        id: snowflake_fulfillment_stock_update_events
        file: models/input_ports/snowflake_fulfillment_stock_update_events.odcs.yaml
    tables:
      - name: STOCK_UPDATES
        description: All updates to the stock
        columns:
          - name: SKU
            description: Stock keeping unit.
            data_type: varchar
          - name: LOCATION
            description: The location where the stock is stored.
            data_type: varchar
          - name: AMOUNT
            description: The number of items in stock (negative for sales/write-offs).
            data_type: number
          - name: TIMESTAMP
            description: Actual timestamp of the stock update event.
            data_type: timestamp_ntz
          - name: PROCESSING_TIMESTAMP
            description: When this event was processed.
            data_type: timestamp_ntz
